Otimizações aplicadas (v9):
1. HTML pré-renderizado: o conteúdo em português já vem pronto no index.html
   (antes a página era 100% montada por JavaScript, atrasando o LCP).
2. CSS crítico embutido em <style> — elimina 1 requisição bloqueante de 58 KB.
3. Google Fonts carregado de forma assíncrona (preload + onload), sem bloquear a renderização.
4. O JS detecta o HTML pré-renderizado (data-prerendered) e apenas ativa as
   interações, evitando re-renderizar tudo no carregamento.
5. Compressão gzip/brotli habilitada no .htaccess.
Observação: ao editar textos em app-krivosheeva-v8.js, gere novamente o HTML
pré-renderizado dentro de <div id="app" data-prerendered="pt"> no index.html.
